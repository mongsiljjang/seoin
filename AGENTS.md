# AGENTS.md — MediOps 프로젝트 규칙 (AI 세션 공통)

이 파일은 새 AI 세션이 가장 먼저 읽는 규칙서다. 세션마다 아래를 지킨다.

## 프로젝트 한 줄
치과 운영 앱(재고·임플란트·출퇴근·연월차). 상용화(SaaS)를 지향한다.

**아직 실제 운영 데이터가 없다** (2026-08-19 사용자 확인). 배포는 되어 있지만 앱 안은 비어 있고, 직원 계정도 전부 본인이 만든 테스트용이다. 그래서 **잃을 것이 없고, 구조를 과감하게 바꿔도 된다.** 실제 데이터가 들어가기 시작하면 이 문단을 고치고, 그때부터 되돌리기 부담을 계산에 넣는다.

## 저장소 · 배포
- 정식본: `mongsiljjang/mediops-pilot` @ `main`
- 개인 백업: `mongsiljjang/seoin` @ `claude/hospital-inventory-hr-app-fw7a9g`
- **두 저장소의 `index.html`은 항상 동일 내용으로 유지**한다(한쪽 수정 시 다른 쪽에도 복사).

### seoin 의 브랜치 — 세션마다 이름이 달라진다
AI 세션은 저마다 `claude/…-xxxxxx` 라는 **새 이름**을 받고 거기에만 푸시할 수 있다.
그래서 위의 백업 브랜치 이름과 실제 작업본이 자주 어긋난다. 규칙은 이렇다.

1. **내용이 가장 앞선 브랜치가 작업본이다.** 이름으로 고르지 않는다.
   `git branch -r` 로 커밋 수를 세고 `git merge-base --is-ancestor` 로 확인한다.
2. 세션은 자기 지정 브랜치를 **작업본 위에서 시작**한다 —
   `git checkout -B <내-브랜치> origin/<작업본>`.
3. 세션이 끝나면 사용자가 백업 브랜치를 그리로 당긴다(조상 관계라 강제 푸시가 필요 없다).

**2026-08-21 확인** — 한때 공통 조상이 없는 두 갈래가 있었으나 지금은 하나로 모였다.
`hospital-inventory-hr-app-fw7a9g` 는 작업본의 조상이고, 옛 22커밋 갈래(`ab7fb8b`)는
어느 브랜치에도 걸려 있지 않다. **머지할 것은 없다.**
- 배포: GitHub Pages → https://mongsiljjang.github.io/mediops-pilot/ (실시간 공유는 Firebase Firestore)

## 개발 원칙
- **Firebase 유지**(Supabase 등으로 이전하지 않는다). 기존 데이터 보존.
- **개인정보 최소.** 환자 정보는 넣지 않는다. 환자 식별이 필요하면 **차트번호(가명)** 만 쓴다(이름·주민번호·전화 금지).
- 화면/페이지를 만들거나 고치면 **`deslop-ko` 스킬로 한글 AI-티(조판·저대비 등)를 점검**한다.
- 작업은 **커밋 후 두 저장소 모두 푸시**한다(designated 브랜치 유지).
- 앱은 서버 없는 정적 웹 + Firebase다. 문자/카톡 알림·거래처 로그인 등 **서버가 필요한 기능은 상용화 Phase로 미룬다**(현재는 인앱 알림만).

## 화면에 내지 않는 것 — 2026-08-22 추가

**앱이 화면에 낼 것은 병원에 도움이 되는 정보이지, 남에게 보여줄 때 불리해지는
기록이 아니다.**

이 앱은 장부와 실물이 왜 안 맞는지 잡아내는 도구가 맞다. 그런데 **잡아낸 것을
그대로 화면에 내걸면 실사 대비 앱처럼 보인다.** 통화록 1-5장에 심평원 실사가
세무조사보다 무겁다고 적혀 있다. 그 두려움 위에 오차 목록을 상시로 띄우면,
실장에게는 감시로, 원장에게는 불리한 증거로 읽힌다. 팔러 간 자리에서 끝난다.

**기능은 안에 두고, 화면에는 업무를 낸다.** 잡아내는 일은 계속하되 그 결과를
간판에 걸지 않는다.

한 세션에 두 번 걸렸다. 둘 다 되돌렸다.

| 걸린 것 | 왜 안 되나 | 어떻게 했나 |
|---|---|---|
| 홈 첫 화면 `오늘 틀어진 것 3건` (빨간 숫자) | 매일 아침 여는 고발장 | 홈을 전으로 되돌림 (`1e30f61` 에 남아 있음) |
| 대사 요약 `손으로 고침 +15 · 매입도 사용도 아닌 몫` | 장부를 손으로 고쳤다는 자백 | 0번 칸 **이월재고**로 바꿈. 보정은 값만 남기고 화면에서 뺌 |

두 번째는 답이 통화록에 있었다. 실장이 번호까지 붙여 말한 다섯 칸의 0번이
비어 있었을 뿐이다. **드러내면 안 되는 숫자가 나오면, 먼저 그것이 실장의 말로
이름이 있는 칸인지 통화록을 뒤진다.** 이름이 있으면 그건 자백이 아니라 회계다.

`test/장부맞춤.mjs` 가 `손으로 고침`·`매입도 사용도 아닌 몫`·`설명 안 되던`
같은 문구가 `index.html` 에 다시 들어오면 깨진다. 무심코 되살릴 수 없다.

## 시험
설치 없이 `node` 로 바로 돈다. 화면 코드를 고쳤으면 셋 다 돌린다.

```
node test/수첩읽기.mjs      node test/단계별수술.mjs      node test/보험페일.mjs
```

보안규칙 시험은 에뮬레이터가 필요하다 — `test/README.md` 참고
(`npm install --legacy-peer-deps`).

세 파일 모두 **`index.html` 에서 함수를 이름으로 떼어 와 돌린다.** 흉내낸 코드를
시험하면 앱이 고장나도 시험은 통과하기 때문이다. 함수 이름을 바꾸면 시험이 먼저 깨진다.

## 문서 위치
- 로드맵: `docs/ROADMAP.md` · 데이터 모델: `docs/DATA_MODEL.md`
- 노무 근거: `docs/LABOR_RULES.md` · **보험 임플란트에 하지 않기로 한 것: `docs/INSURANCE_RULES.md`**
- 실장 통화록(요구사항 원천): `docs/CALL_2026-08-19_REQUIREMENTS.md`
- 세션 핸드오프: `review/session-handoff-YYYY-MM-DD.md` (가장 최근 것을 먼저 읽는다)

## 세션 마무리(wrap) 규칙
`wrap` 스킬을 쓸 때:
1. 두 저장소 상태 정리 + 핸드오프 문서(`review/session-handoff-YYYY-MM-DD.md`) 작성·커밋·푸시.
2. **채팅 마지막에 "다음 세션 시작 프롬프트"를 복사용 코드블록으로 반드시 출력한다.** (핸드오프 파일 안에만 두지 말고, 사용자가 바로 복사할 수 있게 채팅에도 낸다.)
   - 프롬프트에는: 이어서 할 작업 후보(우선순위), 핸드오프 문서 경로, 위 개발 원칙 요약을 담는다.
3. 같은 날 핸드오프가 이미 있으면 `-02` 등 접미사로 새로 만든다. 새 작업이 없으면 새 핸드오프를 만들지 않고 상태만 확인·보고한다.

## 다음 세션 시작 프롬프트 (기본형 — 상황 맞게 갱신)
```
MediOps 치과 앱 이어서 작업. review/ 폴더의 가장 최근 session-handoff 문서를 읽고 시작해줘.
원칙(AGENTS.md): Firebase 유지 · 개인정보 최소(차트번호만·환자정보 X) · 두 저장소 index.html 동일 유지 · 커밋+두 저장소 푸시 · 만든 화면은 deslop-ko로 점검.
다음 후보: 핸드오프의 '다음 작업' 순서를 따른다.
```
