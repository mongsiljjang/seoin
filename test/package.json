{
  "name": "mediops-rules-test",
  "private": true,
  "type": "module",
  "scripts": {
    "emu": "firebase emulators:start --only firestore --project demo-mediops",
    "test": "FIRESTORE_EMULATOR_HOST=127.0.0.1:8080 node rules.권한.mjs && FIRESTORE_EMULATOR_HOST=127.0.0.1:8080 node rules.한계.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^12.0.0",
    "firebase-tools": "^13.0.0"
  }
}
