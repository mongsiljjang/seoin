# 🌐 공유 버전 설정 가이드 (전 직원이 같은 데이터 보기)

이 문서를 따라 하면 **카톡에 올린 링크 하나로 전 직원이 같은 재고·출퇴근·연월차를 실시간으로** 보게 됩니다.
직원들은 **아무 설치·가입도 필요 없어요.** 링크만 누르면 됩니다.

크게 **2가지**만 하면 됩니다.
1. **Firebase** 만들기 (무료 실시간 데이터 저장소) — 약 10분
2. **앱 올리기(호스팅)** 후 링크 공유 — 약 5분

> 😌 어렵게 느껴지시면, 아래 **①~④의 config 6줄만 저(Claude)에게 붙여넣어 주세요.** 제가 파일에 넣고 배포까지 안내해 드릴게요.

---

## 1부. Firebase 만들기 (무료 데이터 저장소)

### ① 프로젝트 만들기
1. https://console.firebase.google.com 접속 (구글 계정으로 로그인)
2. **프로젝트 추가** → 이름 입력(예: `hospital`) → 계속 → (Google 애널리틱스는 꺼도 됩니다) → **프로젝트 만들기**

### ② Firestore 데이터베이스 만들기
1. 왼쪽 메뉴 **빌드 → Firestore Database** → **데이터베이스 만들기**
2. 위치는 기본값 두고, **테스트 모드로 시작** 선택 → 사용 설정
   - (테스트 모드는 30일 뒤 잠깁니다. 실제 사용 전 아래 ⑤ 보안 규칙을 꼭 넣어 주세요.)

### ③ 익명 로그인 켜기
1. 왼쪽 메뉴 **빌드 → Authentication** → **시작하기**
2. **Sign-in method** 탭 → **익명(Anonymous)** 선택 → **사용 설정** → 저장
   - (직원이 로그인 없이 앱을 쓰되, 아무나 인터넷에서 접근하진 못하게 하는 장치예요.)

### ④ 웹 앱 등록하고 설정값 복사
1. 프로젝트 개요(왼쪽 위 톱니 옆) → **프로젝트 설정**
2. 아래로 내려 **내 앱** → **웹(</>)** 아이콘 클릭 → 앱 닉네임 입력 → **앱 등록**
3. 화면에 나오는 `firebaseConfig` 값(apiKey, projectId 등 6줄)을 복사
4. 저장소의 **`firebase-config.js`** 파일을 열어 그 값들을 붙여넣습니다:

```js
window.FIREBASE_CONFIG = {
  apiKey: "여기에_복사한_값",
  authDomain: "여기에_복사한_값",
  projectId: "여기에_복사한_값",
  storageBucket: "여기에_복사한_값",
  messagingSenderId: "여기에_복사한_값",
  appId: "여기에_복사한_값"
};
window.WORKSPACE_ID = "clinic";
```

### ⑤ 보안 규칙 넣기 (중요)
Firestore Database → **규칙(Rules)** 탭에 아래를 붙여넣고 **게시**:

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /hospapp/{docId} {
      allow read, write: if request.auth != null;
    }
  }
}
```

이러면 **앱을 통해 로그인된 사람만** 데이터를 읽고 쓸 수 있습니다.

---

## 2부. 앱 올리기(호스팅) & 링크 공유

앱은 파일 몇 개짜리라 **정적 호스팅**이면 됩니다. 가장 쉬운 방법 하나만 고르세요.

### 방법 A. Netlify Drop (가장 간단, 계정 없이도 가능)
1. GitHub에서 이 저장소를 **ZIP으로 내려받아 압축 해제** (또는 `index.html`, `xlsx.mini.min.js`, `firebase-config.js` 3개 파일이 든 폴더 준비)
2. https://app.netlify.com/drop 접속 → **그 폴더를 마우스로 끌어다 놓기**
3. 몇 초 뒤 나오는 **주소(링크)**를 복사 → 카톡 채팅방에 공유 📎

### 방법 B. Firebase Hosting (Firebase 한 곳에서 관리)
컴퓨터에 Node.js가 있다면 터미널에서:
```
npm install -g firebase-tools
firebase login
firebase init hosting   # 공개 디렉터리는 현재 폴더(.)로, SPA는 아니오
firebase deploy
```
배포 후 나오는 `...web.app` 주소를 공유하세요.

### 방법 C. GitHub Pages
저장소 **Settings → Pages** → 소스 브랜치 선택 → 저장. 몇 분 뒤 나오는 주소를 공유.

---

## 사용 시작
- 링크를 처음 연 사람의 화면 데이터가 공유 저장소에 올라가고, 이후 **모두가 실시간으로 같은 화면**을 봅니다.
- 상단 배지가 **☁︎ 공유중** 이면 정상 연결된 거예요. **이 기기** 로 보이면 설정값이 비었거나 인터넷/규칙 문제입니다.
- 첫 세팅 때 예시 데이터가 올라가 있으면, **설정 → 전체 데이터 초기화** 후 실제 재고를 엑셀로 올리세요.

## 알아두면 좋은 점
- **동시 편집**: 두 사람이 **정확히 같은 순간**에 저장하면 나중 저장이 이깁니다(last-write-wins). 보통 병원 규모에선 문제되지 않지만, 재고 대량 수정은 한 사람이 맡는 걸 권장해요.
- **백업**: 가끔 **설정 → 백업 내보내기**로 파일을 받아두면 안전합니다.
- **보안**: 링크와 `firebase-config.js`를 아는 사람은 접근할 수 있으니, **링크는 병원 카톡방 안에서만** 공유하세요. 더 강한 잠금(직원 PIN 등)이 필요하면 추가해 드릴 수 있습니다.

---

궁금하거나 막히는 단계가 있으면 그 화면을 캡처해서 보내주세요. 이어서 도와드리겠습니다.
